package com.eiip.ticket

import com.hanlin.fadp.entity.GenericDelegator
import com.hanlin.fadp.entity.util.EntityQuery

class TicketUtil {
    def getTicket(GenericDelegator delegator, String ticketId, String ticketType) {
        EntityQuery.use(delegator).from('')
    }
}
